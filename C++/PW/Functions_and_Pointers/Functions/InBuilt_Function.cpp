// min(x,y)     To find minimum of two numbers.
// max(x,y)     To find maximum of two numbers.
// sqrt(x)      To find square root of the number.  ---> #include<cmath>

#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int a=5;
    int b=10;
    int c=49;
    cout << min(a,b) << endl;
    cout << max(a,b) << endl;
    cout << sqrt(c) << endl;
    return 0;
}